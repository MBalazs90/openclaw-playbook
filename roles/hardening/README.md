# hardening

Hardens an Ubuntu server: creates a dedicated `openclaw` system user, randomizes the SSH port, disables root/password login, enables fail2ban, applies 30+ sysctl rules, and configures UFW with default-deny incoming.
